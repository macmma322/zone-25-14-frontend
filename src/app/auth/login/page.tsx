// src/app/auth/login/page.tsx
import LoginClient from "./LoginClient";

export const metadata = {
  title: "Login",
  description: "Embark on the Zone."
};

export default function LoginPage() {
  return <LoginClient />;
}
